# 给 AI Agent 写一个记忆系统：Hindsight-Memory 的架构设计与工程实践

> 开源项目：https://github.com/simer11-jing/hindsight-memory

---

## 一、问题：为什么 Agent 的"记忆"总是做得很糟糕？

过去半年我一直在做 AI Agent 相关的东西，踩过很多坑。其中最让我头疼的，不是模型调用、不是工具编排，而是 **Agent 的记忆系统**。

我试过几种方案：

- **LangChain 的 ConversationBufferMemory**：纯追加，越存越乱，没有去重、没有归档、没有清理。
- **LangChain 的 ConversationSummaryMemory**：每次都要重新总结，token 消耗大，而且总结质量完全依赖模型。
- **MemGPT**：理念很好，但太重了，需要完整的 Agent 框架支撑，不适合轻量场景。
- **自己写一套**：一开始觉得简单，做着做着发现，记忆系统的问题远不止"存和取"。

真正的问题在于：

1. **没有分层**：短期上下文、长期经验、客观事实、核心偏好全部混在一个地方，检索效率低，噪声大。
2. **没有生命周期**：写了就存，没有归档、没有压缩、没有过期策略，记忆库会无限膨胀。
3. **没有检索路由**：语义检索、关键词检索、全文检索各用一套，调用逻辑分散，维护成本高。
4. **没有健康检查**：不知道索引是否新鲜、不知道容量是否超标、不知道最近是否更新。

---

## 二、设计：五层记忆架构

Hindsight-Memory 的核心设计思想是 **分层 + 生命周期 + 统一检索**。

### 2.1 五层结构

```
┌──────────────────────────────────────────────────────┐
│  L1 Ephemeral    瞬时层    临时上下文、任务缓存       │
│  L2 Experiences  经历层    具体事件、会话片段         │
│  L3 Observations 观察层    从经历中提炼的规律模式     │
│  L4 World Facts  事实层    稳定可复用的客观信息       │
│  L5 Mental Models 模型层   高层策略、核心偏好         │
└──────────────────────────────────────────────────────┘
```

每一层有独立的存储和检索策略：

| 层 | 存储方式 | 更新频率 | 检索策略 |
|---|---------|---------|---------|
| L1 | 内存/临时文件 | 实时 | 直接读取 |
| L2 | 按日归档的文件 | 每日 | 全文检索 |
| L3 | 结构化 JSON | 按需 | 关键词 + 语义 |
| L4 | 结构化 JSON | 按需 | 关键词 |
| L5 | 纯文本（MEMORY.md）| 手动 | 索引 + 语义 |

### 2.2 为什么是五层？

这不是凭空设计的，而是从实际使用中总结出来的：

- **L1 和 L2 是"数据"**：需要快速写入、快速读取，不要求高质量。
- **L3 是"信息"**：从数据中提炼，需要结构化存储。
- **L4 是"知识"**：稳定、可复用，需要精确检索。
- **L5 是"认知"**：最高层，需要人工参与提炼，是 Agent 的"世界观"。

这个分层的好处是：**检索时可以直接定位到目标层，不用全库扫描。**

---

## 三、工程实现

### 3.1 统一检索路由

这是整个系统最关键的模块。

```js
// memory-query-router.js
// 检索优先级：KG-Lite 索引 → FTS5 全文 → 语义降级 → 关键词
```

检索流程：

1. **KG-Lite 快速匹配**：MEMORY.md 顶部的实体关系三元组索引，毫秒级响应。
2. **FTS5 全文检索**：SQLite FTS5 索引，覆盖所有归档文件。
3. **语义检索降级**：如果 embedding 不可用，自动回退到 FTS5。
4. **关键词兜底**：所有检索都失败时，用基础关键词搜索。

**设计原则：永远不要让用户面对"检索失败"。**

### 3.2 语义检索的降级机制

```js
// memory-semantic-fallback.js
// embedding 调用失败 → FTS5 → 关键词
```

语义检索最大的问题是 **依赖外部 API**，一旦 API 不可用，整个检索链就断了。

Hindsight-Memory 的做法是：

- 每次检索先检查 embedding API 是否可用
- 不可用时自动切换到 FTS5
- FTS5 也不可用时切换到关键词
- 全程不中断，用户无感知

### 3.3 去重与校验

```js
// memory-dedup.js
// 过滤噪音、重复条目，保证记忆质量
```

记忆系统最怕"熵增"——随着时间推移，噪音越来越多，有用信息越来越难找。

`memory-dedup.js` 负责：

- 检测重复条目（基于文本相似度）
- 过滤噪音行（系统日志、空行、占位符）
- 校验 entry schema（tags、confidence 等字段）

### 3.4 全文索引构建

```python
# build-fts-index.py
# 基于 SQLite FTS5，支持本地快速检索
```

用 Python 而不是 Node.js 的原因：

- Python 的 sqlite3 是标准库，零依赖
- 同步写入，不会出现 Node 异步回调导致的锁问题
- 构建过程可控，适合 cron 定时任务

### 3.5 健康检查

```bash
# memory-healthcheck.sh
# 检查容量、索引、更新时间、进程状态
```

健康检查覆盖：

- MEMORY.md 行数（是否超过 200 行上限）
- STM 文件是否更新（超过 48 小时告警）
- 昨夜归档是否存在
- 压缩日志是否存在
- embedding API 可用性
- FTS5 索引新鲜度
- 跨 Agent 收件箱状态

---

## 四、演进路线

### 4.1 当前版本（v2.1）

- ✅ 五层记忆架构
- ✅ STM → LTM 自动归档
- ✅ 统一检索路由
- ✅ 语义检索降级
- ✅ 去重与校验
- ✅ 健康检查
- ✅ FTS5 全文索引
- ✅ 脚本化集成

### 4.2 下一步计划

1. **向量检索增强**  
   当 embedding API 恢复后，支持向量相似度检索，与 FTS5 做混合排序。

2. **跨 Agent 记忆共享**  
   多 Agent 场景下，共享记忆池（mentalModels、worldFacts 等层）。

3. **CI/CD 集成**  
   加入测试脚本，每次 PR 自动验证记忆读写、检索、归档链路。

4. **可视化面板**  
   简单的 Web 界面，展示记忆库状态、检索结果、健康指标。

---

## 五、为什么选择这个方向？

做 Agent 的人很多，但愿意认真做**记忆系统**的人很少。

大多数 Agent 的记忆，本质上是一个"缓存"——用完就丢，或者越存越乱。  
但一个真正有用的 Agent，必须能记住东西，而且记得**有质量**。

Hindsight-Memory 想做的，不是替代 LangChain、不是替代 MemGPT，  
而是提供一个**轻量、务实、可持续维护**的记忆底座。

如果你也在做 Agent，欢迎一起把"长期记忆"这件事做扎实。

---

## 开源地址

**GitHub：**  
https://github.com/simer11-jing/hindsight-memory

MIT License · 欢迎 Star · 欢迎 PR
